"";             // empty string
'abc';          // single-quoted string
"def";          // double-quoted string

'abc \' def';   // escaped single quote
"abc \" def";   // escaped double quote
"Line\nBreak";  // escaped sequences (line break)
"\x41";         // escaped sequences (hex)
"\u0041";       // escaped sequences (unicode)

"😊";           // unicode character
