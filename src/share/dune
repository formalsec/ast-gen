(include_subdirs unqualified)

(library
 (name graphjs_share)
 (modules site properties taint_config)
 (libraries graphjs_base dune-site)
 (flags
  (:standard -open Graphjs_base)))

(generate_sites_module
 (module site)
 (sites graphjs))
